pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'node --version'
            }
        }
         stage('test') {
            steps {
                sh 'node --version'
            }
        }
         stage('deploy') {
            steps {
                sh 'node --version'
            }
        }
    }
}
