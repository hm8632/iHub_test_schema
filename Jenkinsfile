node {  
    stage('Build') { 
        def branches = [:]
        def names = nodeNames()
        for (int i=0; i<names.size(); ++i) {
            def nodeName = names[i];  
            echo "Triggering on " + nodeName
            }
    }
    stage('Test') { 
         
    }
    stage('Deploy') {  
        
    }
}